{
  "name": "auto-strategy",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "start:dev": "turbo run dev",
    "local:up": "docker compose -f ./docker-compose.local.yaml up -d"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}