model Map {
  id           String        @id @default(uuid())
  size         Json
  terrain_data Json          @map("terrainData")
  spawn_points Json          @map("spawnPoints")
  game_session String        @unique @map("gameSession")
  GameSession  GameSession[]

  @@map("maps")
}
